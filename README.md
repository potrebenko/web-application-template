Domain Layer (Core Business Logic) – Contains entities, value objects, and domain services.

Application Layer (Use Cases & CQRS) – Defines business rules, CQRS commands/queries, and interfaces.

Infrastructure Layer (Implementations & DB Access) – Implements interfaces from the Application Layer.

Presentation Layer (API/UI Controllers) – Exposes the functionality via endpoints or UI.
